query productList($cursor: String) {
  posts(after: $cursor) {
    edges {
      cursor
      node {
        id
        name
        description
        isVoted
        votesCount
        thumbnail {
          url
        }
      }
    }
  }
}
